import React from "react";

import { InfoTextContainer, InfoTitle, InfoTitleBold } from "./styles";

const LogoTitle: React.FC = ({}) => {
  return (
    <InfoTextContainer>
      <InfoTitle>MOBIAUTO</InfoTitle>
    </InfoTextContainer>
  );
};

export default LogoTitle;
